import React from "react";
import { Typography } from "@mui/material";

function Menu() {
  return (
    <div>
      <Typography variant="h4">Menu Page</Typography>
      <Typography variant="body1">This is the menu page.</Typography>
    </div>
  );
}

export default Menu;
